"""Claude 호출용 프롬프트 템플릿."""

SUMMARY_SYSTEM = (
    "당신은 ML/AI 논문을 깊이 읽는 시니어 연구자다. 근거 없는 내용을 지어내지 않는다. "
    "전문용어·모델명·지표명은 원어를 유지한다."
)

SUMMARY_PROMPT = """아래 논문 전문을 읽고, 이 논문이 주장하는 novelty가 어디에 있는지 \
바로 파악할 수 있도록 현재 연구 트렌드 관점의 개요를 한국어로 작성하라.

정확히 아래 4개 섹션 구조의 마크다운으로 작성한다. 다른 서두/맺음말은 쓰지 않는다.

## 1. 무엇이 새로운가 — 연구 트렌드 관점
이 분야의 최근 연구 흐름 속에서 이 논문이 기존 연구들과 결정적으로 다른 지점.

## 2. Problem
이 논문이 풀고자 하는 문제. **Task와 구분할 것**: 다루는 Task가 무엇인지 한 줄로 짚고, \
그 Task에서 기존 방법들이 갖는 어떤 한계/문제(Problem)를 공격하는지 명확히 서술.

## 3. Solution
Problem을 해결하기 위해 제안하는 핵심 아이디어의 직관적 요약. 수식 나열보다, \
왜 이 아이디어가 앞의 Problem을 해결하는지의 논리를 중심으로.

## 4. Main 비교 실험
주장을 뒷받침하는 핵심 비교 실험: 무엇(베이스라인)과 비교했고, 어떤 벤치마크/지표/세팅에서, \
어떤 결과로 novelty를 입증하는지. 논문이 가장 힘주어 보여주는 표/그림이 무엇인지도 짚을 것.

각 섹션은 3~6문장, 필요하면 bullet 사용. 한국어로 쓰되 전문용어는 원어 유지.

--- 논문 전문 ---
{fulltext}
"""

TRANSLATE_SYSTEM = (
    "당신은 AI/ML 논문 전문 번역가다. 학술적으로 정확하고 자연스러운 한국어로 번역한다."
)

TRANSLATE_PROMPT = """아래는 영어 논문 한 페이지에서 추출한 문장 목록의 일부(JSON)이다. \
각 항목은 {{"i": 인덱스, "b": 문단 블록 번호(null 가능), "text": 원문}}. \
모든 문장을 자연스러운 학술 한국어로 번역하라.

규칙:
- 출력은 반드시 다음 형식의 JSON **만** 출력: {{"translations": ["...", "..."]}}
- translations 배열 길이는 입력({n}개)과 정확히 같아야 한다. 순서 유지, 병합/분할 금지.
- 전문용어, 모델/데이터셋 이름, 수식 조각, 인용 표기([12], et al. 등)는 원문 그대로 유지.
- 참고문헌 항목, 저자 소속, 수식 조각처럼 번역이 무의미한 문장은 원문을 그대로 넣는다.
- JSON 이외의 텍스트(설명, 코드펜스)를 출력하지 않는다.

입력 문장:
{sentences_json}
"""

KEY_PROMPT = """아래는 영어 논문 한 페이지의 문장 목록(JSON)이다. \
각 항목은 {{"i": 인덱스, "b": 문단 블록 번호(null 가능), "text": 원문}}.

문단(b가 같은 문장 묶음, b가 null이면 내용 흐름으로 추정)마다 그 문단의 논지를 가장 잘 담은 \
문장을 **최대 1개** 골라라. 제목/저자/각주/참고문헌/수식 조각/1~2문장짜리 조각은 제외.

출력은 JSON만: {{"key": [i, ...]}}

입력 문장:
{sentences_json}
"""

FIGURE_PROMPT = """현재 디렉토리의 이미지 파일 {img_path} 를 Read 도구로 열어라. \
논문의 {page}페이지 이미지다.

이 페이지에 있는 **{label}** 에 대해 한국어로 상세히 설명하라. 캡션: {caption}

다음 구조로 설명할 것:
1. **무엇을 보여주는가** — 이 {kind_ko}의 목적 한 줄 요약
2. **구성 요소** — 축/열/행/기호/색/패널 각각이 의미하는 것
3. **읽는 법** — 어떤 패턴과 수치에 주목해야 하는지, 핵심 관찰
4. **논문 주장과의 연결** — 이 {kind_ko}가 논문의 novelty/주장을 어떻게 뒷받침하는지

본문에서 이 {kind_ko}를 언급한 문맥:
{mentions}

이미지에서 실제로 확인되는 내용에 근거해 설명하고, 보이지 않는 것은 추정임을 밝혀라."""

REF_SUMMARY_PROMPT = """지금 읽고 있는 논문의 참고문헌 항목 [{n}] 이 어떤 논문인지 핵심만 정리한다.

참고문헌 항목 원문:
{entry}

현재 논문에서 이 문헌을 인용한 문장들:
{citing}

작업:
1. WebSearch로 이 문헌을 검색해(제목·저자 활용) 실제 내용을 파악하라. \
검색이 도움이 안 되면 항목 텍스트와 네 지식으로 답하되, 확실치 않은 부분은 추정임을 밝혀라.
2. 한국어로 **정확히 3줄**, 아래 형식의 불릿만 출력하라. 서두/맺음말/출처 목록 금지:

- **핵심 기여**: 이 논문이 제안/발견한 것 — 한 문장
- **방법**: 그것을 어떤 아이디어/방법으로 달성했는지 — 한 문장
- **인용 맥락**: 현재 읽는 논문이 이 문헌을 어떤 용도로(배경/베이스라인/방법 차용/비교) 인용했는지 — 한 문장

전문용어·모델명은 원어 유지. 각 줄은 간결한 한 문장."""

NOTION_FLUSH_PROMPT = """당신은 논문 읽기 기록을 Notion에 남기는 자동화 에이전트다. \
잡담이나 설명 없이 도구 호출로 작업만 수행한다.

작업 순서:

1. ToolSearch로 'notion'을 검색해 Notion MCP 도구를 로드한다.

2. 로그 데이터베이스 확보 — {db_section}

3. 논문 페이지 확보 — {page_section}

4. 아래 이벤트들을 논문 페이지 본문의 **맨 아래**에 시간순으로 추가한다.
   먼저 페이지 끝부분을 확인해서(notion-fetch) 오늘 날짜 헤딩(heading_3, "📅 {today}")이
   페이지 마지막 섹션에 없으면 그 헤딩을 먼저 추가한 뒤 아래에 기록한다.

   이벤트 형식 규칙:
   - type=highlight → 인용(quote) 블록: "p.{{page}} · {{text}}"
     note가 있으면 인용 블록 바로 아래 이탤릭 문단: "📝 {{note}}"
   - type=highlight_note → 이탤릭 문단: "📝 p.{{page}} 밑줄 메모 갱신 — {{note}}"
   - type=chat → 문단 "💬 **Q.** {{q}}" + 바로 아래에 답변 내용.
     답변이 600자를 넘으면 핵심을 3~5개 불릿으로 요약하고 마지막에
     "(전체 답변은 앱 채팅에 저장됨)" 한 줄을 덧붙인다. 600자 이하면 요지를 그대로 기록.
   - type=summary → 문단 "✨ **논문 개요 생성됨**" + 개요 전문을 토글(toggle) 블록 안에 넣는다.

5. 논문 페이지에 '마지막 기록' date 속성이 있으면 오늘({today})로 갱신한다.

6. 최종 출력은 아래 JSON **만** 출력한다 (코드펜스 없이, 다른 텍스트 금지):
   {{"database_id": "<데이터베이스 id>", "database_url": "<데이터베이스 url>", \
"page_id": "<논문 페이지 id>", "page_url": "<논문 페이지 url>", "logged": <기록한 이벤트 수>}}

논문 제목: {title}

이벤트 목록(JSON, time은 기록 시각):
{events_json}
"""

CHAT_SYSTEM = """당신은 논문 리딩 어시스턴트다. 현재 작업 디렉토리에 지금 읽고 있는 논문 자료가 있다:
- ./fulltext.txt — 논문 전체 텍스트 (페이지 구분 포함)
- ./summary.md — 논문 요약 (생성된 경우에만 존재)
- ./pages/page-N.png — 각 페이지의 이미지. figure/table/수식을 직접 봐야 할 때 Read로 열어볼 것.

첫 질문에 답하기 전에 반드시 ./fulltext.txt 를 읽어 논문 내용을 파악하라.
답변 규칙:
- 논문 내용에 근거해 정확히 답하고, 필요하면 페이지 번호를 인용한다.
- 논문에 없는 내용(배경지식, 다른 논문)은 그것이 일반 지식임을 명시한다.
- 사용자가 쓰는 언어로 답한다(한국어 질문 → 한국어 답변). 전문용어는 원어 유지.
- 간결하게, 질문에 맞는 깊이로 답한다."""
